% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_av_fft.R
\name{plot_av_fft}
\alias{plot_av_fft}
\title{Plot a Spectrogram Using av::read_audio_fft}
\usage{
plot_av_fft(
  path_to_file,
  dark = FALSE,
  legend = TRUE,
  keep.par = FALSE,
  useRaster = TRUE,
  vline = NULL,
  max_freq = 16,
  tstart = 0,
  tende = NULL,
  kHz = TRUE,
  delineate_center = NULL,
  fft_window_length,
  overlap,
  ...
)
}
\arguments{
\item{path_to_file}{Character string with the path to the audio file
(e.g., \code{.wav} or \code{.mp3}).}

\item{dark}{Logical, whether to use a dark mode color scheme (default \code{FALSE}).}

\item{legend}{Logical, whether to display a legend with channel and sample rate (default \code{TRUE}).}

\item{keep.par}{Logical, whether to preserve the current graphical parameters (default \code{FALSE}).}

\item{useRaster}{Logical, passed to \code{\link[graphics]{image}} (default \code{TRUE}).}

\item{vline}{Optional numeric vector of time points to draw vertical lines at (default \code{NULL}).}

\item{max_freq}{Numeric, maximum frequency to display (in kHz if \code{kHz = TRUE}, else Hz). Default = 16.}

\item{tstart}{Numeric, start time in seconds (default = 0).}

\item{tende}{Numeric, end time in seconds. If \code{NULL}, uses the duration of the audio.}

\item{kHz}{Logical, whether to display frequency in kHz (default \code{TRUE}).}

\item{delineate_center}{Numeric, optional width in seconds to mark the central region of the spectrogram.}

\item{fft_window_length}{Integer, FFT window length (required).}

\item{overlap}{Numeric between 0 and 1, fraction of window overlap (required).}

\item{...}{Additional arguments passed to \code{\link[graphics]{image}}.}
}
\value{
A base R graphics spectrogram is plotted. The function is called for its side effects
(plotting) and does not return a value.
}
\description{
This function generates a spectrogram from an audio file
using the \pkg{av} package’s FFT functionality. It is a
custom replacement for \code{seewave::spectro()} designed
to integrate into Shiny workflows and R packages.
}
\details{
The function uses \code{av::read_audio_fft()} with a Hann window to compute
the short-time Fourier transform, and then visualizes the result with
\code{\link[graphics]{image}}. Users can optionally display in dark mode,
add vertical guide lines, or highlight the central portion of the spectrogram.
}
\examples{
\dontrun{
# Example with a WAV file
audio_path <- system.file("extdata", "sample.wav", package = "yourPackageName")
plot_av_fft(
  path_to_file = audio_path,
  fft_window_length = 1024,
  overlap = 0.75,
  max_freq = 20
)
}


}
